r = float(input("What is the Radius of the Circle : "))
from math import pi
Area = str(pi*r**2)

print("So finally the AREA of the Circle with the radius" + str(r) + " is --->     " + Area + " units")

